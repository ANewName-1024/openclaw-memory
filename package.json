{
  "name": "@openclaw/memory",
  "version": "1.0.0",
  "description": "OpenClaw Memory System - Persistent, session, and team memory for AI assistants",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "lint": "eslint src/**/*.ts",
    "prepublish": "npm run build"
  },
  "keywords": [
    "openclaw",
    "memory",
    "ai",
    "assistant",
    "persistence"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "zod": "^3.22.0",
    "yaml": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "openclaw": {
    "extension": true,
    "permissions": ["filesystem", "network"]
  }
}
